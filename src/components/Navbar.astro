<<<<<<< Updated upstream
---
const nav = [
  { label: "Home", href: "/" },
  { label: "Projects", href: "/projects" },
  { label: "Design", href: "/design" },
  { label: "About", href: "/about" },
  { label: "Contact", href: "/contact" },
];

const currentPath = Astro.url.pathname;
const normalize = (p) => (p !== "/" && p.endsWith("/") ? p.slice(0, -1) : p);
const isActive = (href) => normalize(currentPath) === normalize(href);
---

<nav class="nav">
  <div class="container nav-inner">
    <a class="brand" href="/">Ripal Patel</a>

    <div class="nav-links">
      {nav.map((item) => (
        <a href={item.href} class={isActive(item.href) ? "active" : ""}>
          {item.label}
        </a>
      ))}
    </div>

    <button id="menuBtn" class="menu-btn" type="button" aria-expanded="false" aria-controls="mobileMenu">
      Menu
    </button>
  </div>

  <div id="mobileMenu" class="mobile">
    <div class="container">
      {nav.map((item) => (
        <a href={item.href} class={isActive(item.href) ? "active" : ""}>
          {item.label}
        </a>
      ))}
    </div>
  </div>

  <script is:inline>
    const btn = document.getElementById("menuBtn");
    const menu = document.getElementById("mobileMenu");
    if (btn && menu) {
      btn.addEventListener("click", () => {
        const open = menu.classList.toggle("show");
        btn.setAttribute("aria-expanded", String(open));
      });
    }
  </script>
=======
<nav class="w-full fixed top-0 left-0 z-50 bg-black/80 backdrop-blur border-b border-white/10">
  <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">
    
    <!-- Logo / Name -->
    <h1 class="text-lg font-semibold tracking-wide">
      Ripal Patel
    </h1>

    <!-- Navigation Links -->
    <ul class="hidden md:flex gap-8 text-sm text-white/80">
      <li><a href="#projects" class="hover:text-indigo-400">Projects</a></li>
      <li><a href="#design" class="hover:text-indigo-400">Design</a></li>
      <li><a href="#skills" class="hover:text-indigo-400">Skills</a></li>
      <li><a href="#contact" class="hover:text-indigo-400">Contact</a></li>
    </ul>

  </div>
>>>>>>> Stashed changes
</nav>
